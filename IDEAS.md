# Ideas

- Create a plugin so skills that should produce a single text artifact nudge the LLM to invoke (a tool?) something that allows the user to open the artifact in their preferred text editor by pressing a single key.
