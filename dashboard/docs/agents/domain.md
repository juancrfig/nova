# Domain Docs

The Dashboard is a room nested inside the Nova repo. Two levels of domain docs:

- **Nova (repo root)**: `../CONTEXT.md` — `Nova`, `pi`, `extension`, `seam`, `room`, `pipes room`.
- **Dashboard (this room)**: `CONTEXT.md` + ADRs in `docs/adr/`.

Before exploring, read the Nova root `CONTEXT.md`, this room's `CONTEXT.md`, and any ADR touching
the area of work. Use the Dashboard glossary's vocabulary (`payload`, `context`, `injectable`,
`seam`, `capture log`, `render surface`). Flag any output that contradicts an ADR explicitly.
